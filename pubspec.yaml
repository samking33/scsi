name: scsi
version: 0.1.0+1
description: Smart Crime Scene Investigation App (Android-first, offline, court-admissible).

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  equatable: ^2.0.5
  uuid: ^4.4.2
  crypto: ^3.0.3
  collection: ^1.18.0
  drift: ^2.18.0
  drift_flutter: ^0.2.8
  sqlite3_flutter_libs: ^0.5.24
  path: ^1.9.0
  path_provider: ^2.1.4
  http: ^1.2.2
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  image_picker: ^1.1.2
  record: ^5.1.2
  pdf: ^3.11.1
  printing: ^5.13.4
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_lints: ^4.0.0
  drift_dev: ^2.18.0
  build_runner: ^2.4.12
