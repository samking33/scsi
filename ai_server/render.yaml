services:
  - type: web
    name: scsi-ai-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    runtime: python
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: YOLO_MODEL
        value: yolov8s.pt
    autoDeploy: true
